function MCSnum = MCSmode2num(MCSmode)

    % input：
    % MCSmode
    % output：
    % MCSnum

    if (MCSmode == 0)
        MCSnum = [6, 1];
    else
        MCSnum = [5, 4];
    end

end
